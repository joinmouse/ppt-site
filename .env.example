# Required for /admin
ADMIN_TOKEN=change-me-to-a-long-random-string
# Optional: kimi.com web key for real PPT generation (empty = mock mode)
KIMI_WEB_KEY=
# Optional overrides
DATA_DIR=/data
DB_PATH=/data/ppt-site.db
